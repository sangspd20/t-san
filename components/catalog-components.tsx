"use client";
import Link from "next/link";
import { ChevronLeft, MoreHorizontal, Plus, Search, SlidersHorizontal, ArrowUp, ArrowDown, ImageIcon } from "lucide-react";
import { Badge, Button, Input } from "@/components/ui";
import { formatVnd, statusLabel } from "@/types/catalog";
import type { CatalogProduct, Category, ModifierGroup, PriceList } from "@/types/catalog";

export function PageHeader({ title, description, action, href }: { title:string; description?:string; action?:string; href?:string }) { return <div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-semibold tracking-tight">{title}</h1>{description && <p className="mt-1 text-sm text-muted-foreground">{description}</p>}</div>{action && href && <Link href={href}><Button><Plus data-icon="inline-start" />{action}</Button></Link>}</div> }
export function SearchBar({ placeholder="Tìm kiếm..." }: { placeholder?:string }) { return <div className="relative max-w-sm"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={16}/><Input className="pl-9" placeholder={placeholder}/></div> }
export function StatusBadge({ status }: { status: "ACTIVE"|"INACTIVE" }) { return <Badge className={status === "ACTIVE" ? "border-primary/20 bg-primary-muted text-primary" : "bg-surface-secondary text-muted-foreground"}>{statusLabel(status)}</Badge> }
export function CatalogToolbar({ filter=true }: { filter?:boolean }) { return <div className="mb-4 flex flex-wrap items-center justify-between gap-3"><SearchBar/><div className="flex gap-2">{filter && <Button variant="outline" size="sm"><SlidersHorizontal data-icon="inline-start"/>Bộ lọc</Button>}<Button variant="outline" size="sm"><MoreHorizontal data-icon="inline-start"/>Thao tác</Button></div></div> }
export function ProductTable({ products }: { products: CatalogProduct[] }) { return <div className="overflow-x-auto rounded-lg border bg-surface"><table className="w-full min-w-[720px] text-sm"><thead className="border-b bg-surface-secondary text-left text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-4 py-3">Sản phẩm</th><th className="px-4 py-3">Danh mục</th><th className="px-4 py-3">Giá bán</th><th className="px-4 py-3">Trạng thái</th><th className="px-4 py-3 text-right">&nbsp;</th></tr></thead><tbody className="divide-y">{products.map(p=><tr key={p.id} className="hover:bg-surface-secondary/60"><td className="px-4 py-3"><Link href={`/products/${p.id}/edit`} className="flex items-center gap-3 font-medium hover:text-primary"><span className="flex size-9 items-center justify-center rounded-md bg-primary-muted text-primary"><ImageIcon size={16}/></span><span><span className="block">{p.name}</span><span className="block text-xs font-normal text-muted-foreground">{p.sku}</span></span></Link></td><td className="px-4 py-3 text-muted-foreground">{p.category}</td><td className="px-4 py-3 font-medium">{formatVnd(p.price)}</td><td className="px-4 py-3"><StatusBadge status={p.status}/></td><td className="px-4 py-3 text-right"><Link href={`/products/${p.id}/edit`} className="text-sm text-primary hover:underline">Chỉnh sửa</Link></td></tr>)}</tbody></table></div> }
export function CategoryTable({ items }: { items: Category[] }) { return <SimpleTable headers={["Tên danh mục","Số sản phẩm","Trạng thái",""]}>{items.map(item=><tr key={item.id}><td className="px-4 py-3 font-medium">{item.name}</td><td className="px-4 py-3 text-muted-foreground">{item.productCount}</td><td className="px-4 py-3"><StatusBadge status={item.status}/></td><td className="px-4 py-3 text-right"><Button variant="ghost" size="sm">Chỉnh sửa</Button></td></tr>)}</SimpleTable> }
export function ModifierTable({ items }: { items: ModifierGroup[] }) { return <SimpleTable headers={["Tên nhóm","Kiểu chọn","Bắt buộc","Số lựa chọn",""]}>{items.map(item=><tr key={item.id}><td className="px-4 py-3 font-medium"><Link className="hover:text-primary" href={`/modifier-groups/${item.id}`}>{item.name}</Link></td><td className="px-4 py-3 text-muted-foreground">{item.type === "SINGLE" ? "Một lựa chọn" : "Nhiều lựa chọn"}</td><td className="px-4 py-3">{item.required ? "Có" : "Không"}</td><td className="px-4 py-3">{item.optionCount}</td><td className="px-4 py-3 text-right"><Link href={`/modifier-groups/${item.id}`} className="text-primary hover:underline">Mở</Link></td></tr>)}</SimpleTable> }
export function PriceListTable({ items }: { items: PriceList[] }) { return <SimpleTable headers={["Tên bảng giá","Kênh bán","Trạng thái","Cập nhật",""]}>{items.map(item=><tr key={item.id}><td className="px-4 py-3 font-medium">{item.name}</td><td className="px-4 py-3 text-muted-foreground">{item.channel}</td><td className="px-4 py-3"><StatusBadge status={item.status}/></td><td className="px-4 py-3 text-muted-foreground">{item.updatedAt}</td><td className="px-4 py-3 text-right"><Button variant="ghost" size="sm">Mở</Button></td></tr>)}</SimpleTable> }
function SimpleTable({ headers, children }: { headers:string[]; children:React.ReactNode }) { return <div className="overflow-x-auto rounded-lg border bg-surface"><table className="w-full min-w-[620px] text-sm"><thead className="border-b bg-surface-secondary text-left text-xs uppercase tracking-wide text-muted-foreground"><tr>{headers.map((h,i)=><th key={i} className="px-4 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{children}</tbody></table></div> }
export function ProductEditor({ product }: { product?: CatalogProduct }) { const isNew=!product; return <div className="max-w-4xl"><Link href="/products" className="mb-5 inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ChevronLeft size={16}/>Quay lại sản phẩm</Link><PageHeader title={isNew ? "Thêm sản phẩm" : "Chỉnh sửa sản phẩm"} description="Thiết lập thông tin, giá bán và các lựa chọn đi kèm."/><div className="flex flex-col gap-5"><section className="rounded-lg border bg-surface p-5"><h2 className="mb-4 font-semibold">Thông tin cơ bản</h2><div className="grid gap-4 sm:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-medium">Tên sản phẩm<Input defaultValue={product?.name}/></label><label className="flex flex-col gap-2 text-sm font-medium">Mã SKU<Input defaultValue={product?.sku}/></label><label className="flex flex-col gap-2 text-sm font-medium sm:col-span-2">Danh mục<Input defaultValue={product?.category} placeholder="Chọn danh mục"/></label></div></section><section className="rounded-lg border bg-surface p-5"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-semibold">Biến thể và giá</h2><p className="mt-1 text-sm text-muted-foreground">Mỗi biến thể có thể có giá và mã SKU riêng.</p></div><Button variant="outline" size="sm"><Plus data-icon="inline-start"/>Thêm biến thể</Button></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="border-b text-left text-xs text-muted-foreground"><tr><th className="pb-3">Tên biến thể</th><th className="pb-3">SKU</th><th className="pb-3">Giá bán</th></tr></thead><tbody>{(product?.variants ?? [{name:"Tiêu chuẩn",price:0,sku:""}]).map((v,i)=><tr key={i} className="border-b last:border-0"><td className="py-3 pr-3"><Input defaultValue={v.name}/></td><td className="py-3 pr-3"><Input defaultValue={v.sku}/></td><td className="py-3"><Input defaultValue={v.price || undefined} type="number"/></td></tr>)}</tbody></table></div></section><section className="rounded-lg border bg-surface p-5"><h2 className="mb-4 font-semibold">Kênh bán hàng</h2><div className="flex flex-wrap gap-5 text-sm"><label className="flex items-center gap-2"><input type="checkbox" defaultChecked/> Tại quầy</label><label className="flex items-center gap-2"><input type="checkbox" defaultChecked/> Giao hàng</label><label className="flex items-center gap-2"><input type="checkbox"/> Mang đi</label></div></section></div><div className="sticky bottom-0 mt-6 flex justify-end gap-2 border-t bg-background/95 py-4"><Link href="/products"><Button variant="outline">Hủy</Button></Link><Button>Lưu sản phẩm</Button></div></div> }
export function ModifierEditor({ group }: { group: ModifierGroup }) { return <div className="max-w-3xl"><Link href="/modifier-groups" className="mb-5 inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ChevronLeft size={16}/>Quay lại nhóm tuỳ chọn</Link><PageHeader title={group.name} description="Quản lý các lựa chọn và giá cộng thêm."/><section className="rounded-lg border bg-surface p-5"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-semibold">Các lựa chọn</h2><p className="mt-1 text-sm text-muted-foreground">Kéo thứ tự hoặc dùng nút mũi tên để sắp xếp.</p></div><Button variant="outline" size="sm"><Plus data-icon="inline-start"/>Thêm lựa chọn</Button></div><div className="divide-y">{group.options.map((option,i)=><div className="flex items-center gap-3 py-3" key={option.id}><div className="flex flex-col"><button aria-label="Di chuyển lên" disabled={i===0}><ArrowUp size={14}/></button><button aria-label="Di chuyển xuống" disabled={i===group.options.length-1}><ArrowDown size={14}/></button></div><Input className="flex-1" defaultValue={option.name}/><Input className="w-32" type="number" defaultValue={option.price}/><StatusBadge status={option.active ? "ACTIVE" : "INACTIVE"}/><Button variant="ghost" size="sm"><MoreHorizontal/></Button></div>)}</div></section><div className="sticky bottom-0 mt-6 flex justify-end gap-2 border-t bg-background/95 py-4"><Link href="/modifier-groups"><Button variant="outline">Hủy</Button></Link><Button>Lưu thay đổi</Button></div></div> }
